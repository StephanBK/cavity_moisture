# Copy to .env for local dev. NEVER commit the real .env.
# On Railway these are set as service variables, not files.
MAPBOX_TOKEN=pk.your_publishable_mapbox_token
NREL_API_KEY=your_nrel_nsrdb_api_key
